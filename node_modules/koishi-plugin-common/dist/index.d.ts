import { Context } from 'koishi-core';
import admin from './admin';
import authorize, { AuthorizeOptions } from './authorize';
import broadcast, { BroadcastOptions } from './broadcast';
import contextify from './contextify';
import echo from './echo';
import exec from './exec';
import exit from './exit';
import help from './help';
import info, { InfoOptions } from './info';
import repeater, { RepeaterOptions } from './repeater';
import requestHandler, { HandlerOptions } from './request-handler';
import respondent, { Respondent } from './respondent';
import welcome, { WelcomeMessage } from './welcome';
export * from './admin';
export * from './info';
export { admin, authorize, broadcast, contextify, echo, exec, exit, help, info, repeater, requestHandler, respondent, welcome, AuthorizeOptions, BroadcastOptions, HandlerOptions, InfoOptions, RepeaterOptions, Respondent, WelcomeMessage, };
export interface Config extends AuthorizeOptions, BroadcastOptions, HandlerOptions, InfoOptions {
    admin?: boolean;
    broadcast?: boolean;
    contextify?: boolean;
    echo?: boolean;
    exec?: boolean;
    exit?: boolean;
    help?: boolean;
    info?: boolean;
    repeater?: RepeaterOptions;
    respondent?: Respondent[];
    welcomeMessage?: WelcomeMessage;
}
export declare const name = "common";
export declare function apply(ctx: Context, options?: Config): void;
