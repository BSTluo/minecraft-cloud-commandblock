import { Context } from 'koishi-core';
declare type SessionSwitch = boolean | ((repeated: boolean, times: number, message: string) => boolean);
declare type SessionText = string | ((userId: number, message: string) => string);
export interface RepeaterOptions {
    repeat?: SessionSwitch;
    interrupt?: SessionSwitch;
    repeatCheck?: SessionSwitch;
    interruptCheck?: SessionSwitch;
    interruptText?: SessionText;
    repeatCheckText?: SessionText;
    interruptCheckText?: SessionText;
}
export default function apply(ctx: Context, options: RepeaterOptions): void;
export {};
