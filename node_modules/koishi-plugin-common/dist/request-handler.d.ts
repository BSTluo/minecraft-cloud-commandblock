import { App, Meta } from 'koishi-core';
declare type RequestHandler = string | boolean | ((meta: Meta<'request'>, app: App) => string | boolean | void | Promise<string | boolean | void>);
export interface HandlerOptions {
    handleFriend?: RequestHandler;
    handleGroupAdd?: RequestHandler;
    handleGroupInvite?: RequestHandler;
}
export default function apply(ctx: App, options?: HandlerOptions): void;
export {};
